<!-- TrainView.xaml -->
<UserControl x:Class="CTADispatchSim.Views.TrainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid>
        <Border CornerRadius="10" BorderBrush="Black" BorderThickness="2" Background="LightGray" Padding="10">
            <StackPanel>
                <TextBlock Text="Train Information" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Train Name: " FontWeight="Bold"/>
                <TextBlock Text="{Binding Name}" Margin="10,0,0,5"/>
                <TextBlock Text="Current Station: " FontWeight="Bold"/>
                <TextBlock Text="{Binding CurrentStation}" Margin="10,0,0,5"/>
                <TextBlock Text="Direction: " FontWeight="Bold"/>
                <TextBlock Text="{Binding Direction}" Margin="10,0,0,5"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>